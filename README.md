# scaffold-openapi-react

Generate a Vite + React frontend from a running OpenAPI schema.

## Usage

```bash
npm install
npm run build
node ./dist/index.js create http://localhost:8000/openapi.json ./my-new-app --app-name "My App"
```
